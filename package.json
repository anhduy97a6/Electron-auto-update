{
  "name": "electron-demo-auto-update",
  "version": "1.0.2",
  "description": "test auto update",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/anhduy97a6/Electron-auto-update.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^12.0.5",
    "electron-builder": "^22.10.5"
  },
  "dependencies": {
    "electron-updater": "^4.3.8"
  }
}
